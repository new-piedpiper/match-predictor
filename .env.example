# --- Local dev (recommended): same origin as Vite → no CORS / OPTIONS preflight ---
# Vite proxies /api → http://localhost:8080 (see vite.config.ts server.proxy).
VITE_API_BASE_URL=http://localhost:5173

# --- Alternative: call Spring directly (requires CORS on backend) ---
# VITE_API_BASE_URL=http://localhost:8080

# Supabase
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
